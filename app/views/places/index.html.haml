%section.places-index.wrapper.clearfix
  .content-box
    %section.places-index__title
      = icon('fas','map-marked-alt')
      %h2 お店を探す
    %section.places-index__content.clearfix
      .places-index__content__left
        #map
          %p Google mapを読み込み中...        (10秒以上待っても表示されない場合は、ページをリロードしてください)
      .places-index__content__right
        %input{id: "places-search-input", class: "controls", type: "text", placeholder: "キーワードでお店を検索"}
        #places-search-result
        .place-details
          .place-details__name
            %h4
            %p
          .place-details__address
            %h4
            %p
          .place-details__opening-hours
            %h4
            %p.mon
            %p.tue
            %p.wed
            %p.thr
            %p.fri
            %p.sat
            %p.sun
          .place-details__phone-number
            %h4
            %p
          .place-details__price-level
            %h4
            %p
          .place-details__rating
            %h4
            %p
          .place-details__website
            %h4
            %p
    %section.places-index__btn.current-place-btn
      %p 現在地を表示
